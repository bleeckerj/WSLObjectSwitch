Pod::Spec.new do |s|
  # pod customization goes in here
s.name = 'WSLObjectSwitch'
s.version = '1.0.0'
s.summary = 'Some guy made a thing to do syntactically saccarine object matching that looks like a switch statement'
s.author = {'Stephen Darlington' => 'http://www.wandlesoftware.com/'}
s.source = { :git => 'https://github.com/sdarlington/WSLObjectSwitch'}
s.source_files = 'WSLObjectSwitch.{h,m}'
 

end
